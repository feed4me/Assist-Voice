<?xml version="1.0" encoding="utf-8"?>
<!-- The list's title + status line, as the RecyclerView's own first item
     instead of separate views above it — same reasoning as
     item_picker_header.xml: the RecyclerView needs to stay the one
     full-screen view (match_parent x match_parent, no sibling taking up
     part of the screen first), or CurvedScrollIndicatorView draws its arc
     against the wrong (smaller) bounds and rotary focus gets flaky. -->
<!-- paddingTop is round-bezel clearance (stacks with itemsRecycler's own
     paddingTop) — the header is always position 0, so it's always the
     thing that would otherwise sit right at the top curve. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingTop="@dimen/scroll_bottom_extra"
    android:paddingBottom="@dimen/space_m">

    <TextView
        android:id="@+id/headerTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:textAppearance="@style/TextAppearance.App.Label"
        android:text="" />

    <TextView
        android:id="@+id/headerStatus"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/space_xs"
        android:gravity="center"
        android:textAppearance="@style/TextAppearance.App.Hint"
        android:text="" />

</LinearLayout>
