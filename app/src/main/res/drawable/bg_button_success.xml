<?xml version="1.0" encoding="utf-8"?>
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@color/ripple_success">
    <item>
        <selector xmlns:android="http://schemas.android.com/apk/res/android">
            <!-- Disabled while editing the other slot (see
                 SmartHomeDeviceCommandsActivity) — reads as clearly
                 inactive rather than identical-but-unresponsive. -->
            <item android:state_enabled="false">
                <shape android:shape="rectangle">
                    <solid android:color="@color/bg_surface_alt" />
                    <stroke android:width="1dp" android:color="@color/divider_strong" />
                    <corners android:radius="@dimen/button_radius" />
                </shape>
            </item>
            <item>
                <shape android:shape="rectangle">
                    <solid android:color="@color/success_soft" />
                    <stroke android:width="1dp" android:color="@color/success" />
                    <corners android:radius="@dimen/button_radius" />
                </shape>
            </item>
        </selector>
    </item>
</ripple>
